{
  "name": "hello_typescript",
  "version": "0.0.1",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^13.0.0",
    "tsd": "^0.6.5",
    "tslint": "^3.3.0",
    "typescript": "^1.7.5",
    "watchy": "^0.6.5"
  },
  "scripts": {
    "tsc": "tsc --outFile build/ts_out.js src/app.ts",
    "browserify": "browserify build/ts_out.js -o build/bundle.js -s app",
    "build": "npm run tsc && npm run browserify",
    "watch": "watchy -w src -- npm run build",
    "exec": "exec"
  }
}
