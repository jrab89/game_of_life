{
  "name": "hello_typescript",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^1.7",
    "http-server": "^0.8.5",
    "browserify": "^13.0"
  },
  "scripts": {
    "tsc": "tsc app.ts",
    "browserify": "browserify app.js -o bundle.js -s app",
    "listen": "http-server",
    "all": "npm run tsc &&  npm run browserify && npm run listen"
  }
}
